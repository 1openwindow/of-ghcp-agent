# local secrets and azd env
.env
.env.*
!.env.example
.azure/

# generated/runtime files
node_modules/
*.log

# open-foundry deploy skill — installed via `gh skill install`; keep the sample fresh BYO
/.agents/skills/open-foundry/

# open-foundry deploy output — regenerated on every deploy; keep the sample fresh BYO
Dockerfile
.dockerignore
azure.yaml
agent.yaml
agent.manifest.yaml

# editor/os
.DS_Store
.vscode/
.idea/
